@page "/"
@inject NavigationManager Navigation

<h3>Home Page</h3>

@code {
    protected override void OnInitialized()
    {
        // Redireciona para /pacientes assim que o componente for inicializado
        Navigation.NavigateTo("/pacientes", true);
    }
}
